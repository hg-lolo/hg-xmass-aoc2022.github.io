name: Refresh Feed
on: [push]
jobs:
  refresh-feed:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch API Data 📦
        uses: JamesIves/fetch-api-data-action@v2
        with:
          endpoint: https://adventofcode.com/2022/leaderboard/private/view/289461.json
          configuration: '{ "method": "GET", "headers": {"Cookie": "session=${{ secrets.aoctoken.COOKIE }}"} }'
